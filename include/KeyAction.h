#ifndef KEY_ACTION// header guard to prevent multiple includes
#define KEY_ACTION

class Key{
    public:
        enum Action{action_right, action_left, action_shoot, action_quit, no_action, action_restart};
};



#endif